import React, { FC } from 'react';

type AccountDetailProps = {};

const AccountDetail: FC<AccountDetailProps> = (props) => {
    return <div>I am AccountDetail</div>;
};

export default AccountDetail;
